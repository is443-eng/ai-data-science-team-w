# Agent prompts (Segment 4)

Place per-agent system prompts and guardrails here (e.g. `agent_1_tools.md`, `agent_2_state.md`).

The app should load these via a small loader in `ollama_client` or a dedicated `prompts/loader.py` when Segment 4 is implemented.
